# 配置已迁移至 world.config.yaml，见 world.config.example.yaml
#
# 如需临时覆盖配置，可使用环境变量（优先级高于 world.config.yaml）：
#   WORLD_SERVER_PORT  — 服务端端口
#   WORLD_WS_URL       — 客户端连接地址
#   LLM_BASE_URL       — LLM API 地址
#   LLM_API_KEY        — API 密钥
#   LLM_MODEL          — 默认模型
#   WORLD_FILE         — 世界文件路径
#   WORLD_LOG_LEVEL    — 日志级别 (info|warn|evt|perf|ws|key|dbg)
